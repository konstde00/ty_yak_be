databaseChangeLog:
  -  preConditions:

  - changeSet:
      id: usersRolesAddForeignKeyOnUserId
      author: konstde00
      changes:
        - addForeignKeyConstraint:
            baseColumnNames: user_id
            baseTableName: users_invitations
            constraintName: users_invitations_users_FK
            onDelete: CASCADE
            onUpdate: CASCADE
            referencedColumnNames: id
            referencedTableName: users
            validate: true